# API base URL for the control plane.
# Leave empty to use the Vite dev proxy (default for local dev).
# Set this when the dashboard is served separately from the control plane,
# e.g. VITE_API_BASE=http://localhost:8000
VITE_API_BASE=
